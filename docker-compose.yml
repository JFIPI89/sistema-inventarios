services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: inventarios
      POSTGRES_PASSWORD: inventarios
      POSTGRES_DB: inventarios
    volumes:
      - postgres_data:/data

volumes:
  postgres_data:
